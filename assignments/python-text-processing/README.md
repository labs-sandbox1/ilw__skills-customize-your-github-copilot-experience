# 📘 Assignment: Python Text Processing

## 🎯 Objective

Learn to manipulate text data using Python strings, file I/O operations, and text processing techniques to analyze and transform text files.

## 📝 Tasks

### 🛠️ Word Frequency Counter

#### Description
Create a program that reads a text file and analyzes the frequency of words appearing in it. Your program should handle different cases, punctuation, and provide meaningful statistics about the text.

#### Requirements
Completed program should:

- Read text from a file specified by the user
- Convert all text to lowercase for accurate counting
- Remove punctuation marks from words
- Count the frequency of each unique word
- Display the top 10 most common words with their counts
- Calculate and display total word count and unique word count


### 🛠️ Text File Transformer

#### Description
Build a program that performs various transformations on text files, including case conversion, word replacement, and line manipulation. The user should be able to choose which transformation to apply.

#### Requirements
Completed program should:

- Read content from an input text file
- Provide a menu with at least 3 transformation options (e.g., uppercase, reverse lines, replace words)
- Apply the selected transformation to the text
- Write the transformed text to a new output file
- Display a confirmation message showing input/output file names
- Handle file errors gracefully with appropriate error messages
